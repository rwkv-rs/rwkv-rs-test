[project]
name = "albatross-trace"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "ninja>=1.13.0",
    "numpy",
    "packaging>=26.2",
    "safetensors",
    "torch",
    "tqdm",
]
